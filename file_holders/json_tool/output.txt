{
  "$schema": "https://json-schema.org",
  "title": "EnterpriseOrderSchema",
  "type": "object",
  "required": ["orderId", "metadata", "customer", "billingAddress", "lineItems", "financials", "status"],
  "additionalProperties": false,
  "properties": {
    "orderId": {
      "type": "string",
      "format": "uuid"
    },
    "metadata": {
      "type": "object",
      "required": ["timestamp", "environment", "version"],
      "properties": {
        "timestamp": {
          "type": "string",
          "format": "date-time"
        },
        "environment": {
          "type": "string",
          "enum": ["development", "staging", "production"]
        },
        "version": {
          "type": "number",
          "minimum": 1.0
        }
      }
    },
    "customer": {
      "type": "object",
      "required": ["customerId", "email", "isPremium"],
      "properties": {
        "customerId": {
          "type": "string",
          "pattern": "^CUST-\\d{5}$"
        },
        "email": {
          "type": "string",
          "format": "email"
        },
        "isPremium": {
          "type": "boolean"
        },
        "preferences": {
          "type": "object",
          "required": ["marketingOptIn", "currency"],
          "properties": {
            "marketingOptIn": { "type": "boolean" },
            "currency": { "type": "string", "pattern": "^[A-Z]{3}$" }
          }
        }
      }
    },
    "billingAddress": {
      "type": "object",
      "required": ["street", "city", "postalCode", "countryCode"],
      "properties": {
        "street": { "type": "string" },
        "suite": { "type": "string" },
        "city": { "type": "string" },
        "postalCode": { "type": "string", "minLength": 3 },
        "countryCode": { "type": "string", "minLength": 2, "maxLength": 2 }
      }
    },
    "lineItems": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["sku", "name", "quantity", "price"],
        "properties": {
          "sku": { "type": "string", "pattern": "^[A-Z]{4}-[A-Z0-9]{4}-[A-Z0-9]$" },
          "name": { "type": "string" },
          "quantity": { "type": "integer", "minimum": 1 },
          "price": { "type": "number", "minimum": 0.01 },
          "tags": {
            "type": "array",
            "items": { "type": "string" }
          }
        }
      }
    },
    "financials": {
      "type": "object",
      "required": ["subtotal", "taxRate", "total"],
      "properties": {
        "subtotal": { "type": "number", "minimum": 0.0 },
        "taxRate": { "type": "number", "minimum": 0.0, "maximum": 1.0 },
        "discountCode": { "type": "string" },
        "total": { "type": "number", "minimum": 0.0 }
      }
    },
    "status": {
      "type": "string",
      "enum": ["PENDING", "PROCESSING", "SHIPPED", "CANCELLED"]
    }
  }
}
